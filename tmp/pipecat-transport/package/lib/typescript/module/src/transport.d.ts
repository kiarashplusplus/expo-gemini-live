import { DailyCall, DailyCallOptions, DailyFactoryOptions } from '@daily-co/react-native-daily-js';
import { PipecatClientOptions, RTVIMessage, Tracks, Transport, TransportState } from '@pipecat-ai/client-js';
import { MediaDeviceInfo } from '@daily-co/react-native-webrtc';
export declare class RNDailyTransport extends Transport {
    private _dailyWrapper;
    private _daily;
    private _dailyFactoryOptions;
    private _botId;
    private _selectedCam;
    private _selectedMic;
    private _selectedSpeaker;
    constructor(opts?: DailyFactoryOptions);
    initialize(options: PipecatClientOptions, messageHandler: (ev: RTVIMessage) => void): void;
    get dailyCallClient(): DailyCall;
    get state(): TransportState;
    private set state(value);
    getSessionInfo(): import("@daily-co/react-native-daily-js").DailyMeetingSessionSummary;
    getAllCams(): Promise<MediaDeviceInfo[]>;
    updateCam(camId: string): void;
    get selectedCam(): MediaDeviceInfo | Record<string, never>;
    getAllMics(): Promise<MediaDeviceInfo[]>;
    updateMic(micId: string): void;
    get selectedMic(): MediaDeviceInfo | Record<string, never>;
    getAllSpeakers(): Promise<MediaDeviceInfo[]>;
    updateSpeaker(speakerId: string): void;
    get selectedSpeaker(): MediaDeviceInfo | Record<string, never>;
    enableMic(enable: boolean): void;
    get isMicEnabled(): boolean;
    enableCam(enable: boolean): void;
    get isCamEnabled(): boolean;
    enableScreenShare(enable: boolean): void;
    get isSharingScreen(): boolean;
    tracks(): Tracks;
    preAuth(dailyCallOptions: DailyCallOptions): Promise<void>;
    initDevices(): Promise<void>;
    _validateConnectionParams(connectParams?: unknown): DailyCallOptions | undefined;
    _connect(connectParams?: DailyCallOptions): Promise<void>;
    sendReadyMessage(): Promise<void>;
    private attachEventListeners;
    _disconnect(): Promise<void>;
    sendMessage(message: RTVIMessage): void;
    private handleAppMessage;
    private handleAvailableDevicesUpdated;
    private handleSelectedDevicesUpdated;
    private handleDeviceError;
    private handleTrackStarted;
    private handleTrackStopped;
    private handleParticipantJoined;
    private handleParticipantLeft;
    private handleLocalAudioLevel;
    private handleRemoteAudioLevel;
    private handleLeftMeeting;
    private handleFatalError;
    private handleNonFatalError;
}
//# sourceMappingURL=transport.d.ts.map